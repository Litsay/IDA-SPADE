# IDA-SPADE runtime requirements (Python 3.10+ recommended)
# Pinned versions match the configuration used in the paper experiments.

# Core ML
torch==2.8.0
numpy>=1.26,<2.0
pandas==2.3.0
scikit-learn>=1.3,<1.7
scipy>=1.11

# Streaming evaluation utilities
river==0.21

# Pattern causality engine (for PC-DriftForecasting)
pattern-causality>=0.1.5

# Plotting (figures)
matplotlib>=3.7
seaborn>=0.12

# t-SNE / dimensionality reduction
openTSNE>=1.0

# Optional: faster nearest neighbor / clustering
# faiss-cpu>=1.7
